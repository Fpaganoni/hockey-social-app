export enum Role {
  PLAYER = "player",
  COACH = "coach",
  CLUB = "club",
}

export enum Position {
  GOALKEEPER = "goalkeeper",
  DEFENDER = "defender",
  MIDFIELDER = "midfielder",
  ATTACKER = "attacker",
}
